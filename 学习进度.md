# 学习进度记录

> 更新日期：2026-07-14
> 目标方向：Python 工程化 + LLM 代码审查助手 + 前端缺陷观测能力

---

## 已学习内容（已完成）

### 1. Python 基础到工程化思维
- 理解了 `print` 和 `return` 的区别：
  - `print` 是副作用（输出）
  - `return` 才是函数结果（可复用）
- 理解了函数返回值一致性的意义（避免分支类型不一致）。
- 掌握了 `list` / `dict` 的常见使用场景。

### 2. 空值与异常处理
- 掌握了 `None`（不是 `null` / `Node`）。
- 掌握了：
  - `dict["key"]`：必填字段，不存在会 `KeyError`
  - `dict.get("key")`：可选字段，不存在返回 `None` 或默认值
- 认识并处理过：
  - `ValueError`（如 `int("abc")`）
  - `TypeError`（如 `int(None)`）
- 知道 `except Exception as e` 与裸 `except:` 的区别。

### 3. 文件读取与路径
- 掌握 `open(..., "r", encoding="utf-8")` + `readlines()`。
- 理解了相对路径是相对“当前工作目录（cwd）”，不是相对脚本文件。
- 使用 `Path(__file__).parent` 规避 cwd 差异问题。

### 4. 脚本结构化
- 能将脚本拆分为：
  - `openfile`（I/O）
  - `parse_lines/parse_line`（解析）
  - `get_adult_names`（业务规则）
- 理解了“单一职责”与可复用性。

### 5. `__name__` / `main` / import 行为
- 理解了：
  - 直接运行文件：`__name__ == "__main__"`
  - 被导入：`__name__ == 模块名`
- 理解了 `if __name__ == "__main__"` 只控制入口逻辑，不是全局异常防护。

### 6. argparse 入门与常见坑
- 会使用 `ArgumentParser + add_argument + parse_args`。
- 解决过模块遮蔽问题：脚本名不要与标准库重名（如 `argparse.py`）。
- 已能跑通命令：
  - `--file`
  - `--min_age`
  - `--verbose`

### 7. 日志能力（已开始）
- 已接入基础日志：`logging`。
- 已接入按天切分：`TimedRotatingFileHandler`。
- 已能输出结构化日志（时间、级别、内容）。

---

## 当前进行中（进行中）

### 1. 脚本健壮性与退出码
- 已理解 `return 0/1/2` 的价值。
- 需要继续规范：
  - 成功、参数错误、运行异常的统一退出码策略。

### 2. CLI 可维护性
- 已能运行参数化脚本。
- 需要继续：
  - 统一参数命名风格（例如 `--min-age` vs `--min_age`）
  - 更友好的 help 文案与默认值设计。

---

## 待学习清单（下一阶段）

### A. Python 工程化（优先级高）
- 类型标注一致性（`Path` / `str`、`Optional` 等）。
- 更清晰的模块拆分（业务逻辑与入口分离）。
- 单元测试最小闭环（先 3~5 条关键逻辑测试）。

### B. 任务调度与自动化（优先级高）
- Windows Task Scheduler 任务配置标准化：
  - Python 绝对路径
  - 脚本绝对路径
  - Start in
  - 输出重定向日志

### C. 前端缺陷观测（优先级中高）
- 客户偶发 defect 的日志闭环：
  - 前端统一日志入口
  - 环形缓冲区
  - 出错时上报最近用户行为轨迹
  - 脱敏与采样

### D. LLM 应用落地（优先级中）
- 在已有脚本能力上接入 LLM 流程：
  - 结构化输入输出
  - 缺陷模式检索
  - 可解释审查建议

---

## 下次开学入口（直接从这里继续）

1. 打开 `python/test3.py`，确认以下四项是否统一：
- 参数命名
- 退出码
- 异常分层
- 日志路径

2. 完成 3 条最小单元测试（先不求全）：
- `parse_line` 正常输入
- `parse_line` 缺失/脏数据输入
- `get_adult_names` 混合数据过滤

3. 运行并记录一次完整命令链：
- 正常输入
- 文件不存在
- 非法年龄

4. 将“前端 defect 追踪字段模板”补到单独文档（下一次可继续）。

---

## 快速状态摘要（给下次 session）

- 当前已从“语法学习”进入“可维护脚本阶段”。
- 已具备：参数化执行 + 文件处理 + 异常处理 + 基础日志。
- 下一个关键里程碑：
  - 做出“可测、可调度、可排障”的稳定脚本版本。
